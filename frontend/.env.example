# Leave empty for dev (Vite proxy to backend). Set for production builds.
VITE_API_URL=

# Required — Maps JavaScript API key (restrict by HTTP referrer in Google Cloud Console)
VITE_GOOGLE_MAPS_API_KEY=

# Optional — Map ID for Advanced Markers / cloud map styling
# VITE_GOOGLE_MAPS_MAP_ID=
